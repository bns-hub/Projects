# TenderBoard GitHub Actions test

This is a non-recurring connectivity test. A successful run uploads
`TenderBoard_Raw_test.csv` as a workflow artifact. It does not write to Google
Drive and does not alter the existing Claude tasks.
